# 需求拆解
用户随心输入需求/目标，AI 自动拆解成为最多3级任务
各级目标可自由拖拽排序，真实物理反馈
# 自动记录
开启工作模式后，用户打开一个网页后，自动记录该网页信息，自动加入分析队列，由 tabbit 内置 AI 并行分析处理并归档至相关目标
## 工作模式
同时展示工作目标和摸鱼池塘下的记录，额外增加“专注”功能，被无关网页吸引走会被记录进摸鱼分类
## 摸鱼模式
只展示摸鱼池塘下的记录，记录今天摸鱼多少次
## 记录分析提示词
可在设置中编辑预设提示词，让 AI 按照你的想法来分析
# 智能 ToDo list
消息气泡，初始折叠，显示当前建议， AI 根据已有记录分析各个项目的进展，更新ToDo list，并给出下一步建议，让用户跟着指引就能完成像样的研究
## 当前关联网址
ToDo list 可以直接复制下一步要搜索的关键词，并且支持在关联网址下一键跳转搜索
在拾知面板底部填入裸域名(如zhihu.com、juejin.cn、csdn.net、read.douban.com、medium.com、维基百科等)，作为“搜索”按钮的默认跳转目标，拾知会自动识别并直达搜索结果页
# 记录导出
点击导出记录（可选全部目标/指定目标），导出为 json 格式
# 本地 Agent 辅助分析
提示用户可下载 skill 辅助本地 Agent 进行分析
## 配套 skill
描述：接收用户从拾知浏览器扩展导出的记录（JSON），按目标/摸鱼/分析中/分析失败等分类归档到本地目录，同步到 SQLite 数据库，并在用户要求时汇总指定分类生成深度报告。

什么时候使用：当用户发送拾知导出的记录文件（shizhi-export*.json）、要求整理/归档浏览器记录、或要求基于已归档记录生成报告时触发。

功能：
- 归档：按 JSON 中的分类结构，将记录放入本地 ~/.shizhi/archive/
- 入库：同步写入 SQLite 数据库，便于后续查询和报告生成
- 报告：根据用户指定的目标或分类，读取相关记录并生成结构化深度报告
# 输入补全
在 Tabbit 首页的输入框中添加 AI 文本建议自动补全
# 用户画像
定时更新用户画像，作为 AI 记忆，影响 AI 分析、建议、补全功能
# 问问 Deepseek Harness
选中网页文字后右键，把内容直接带进本地 DeepSeek Harness 继续提问。
# 存储说明
Tabbit 脚本妙招只支持同源存储，每个源下的网站共享所有目标与记录，跨源不可见
存储完全本地，打破云端的围墙，你浏览的，就是你的