# Page Topology

1. Fixed header/navigation overlay (transparent at top, blurred dark after scrolling).
2. Hero (`750px` desktop, `577px` mobile): announcement badge, logo/title, subtitle, CTA buttons, platform note, interactive desktop-app mockup, warm radial glow.
3. Sponsor callout (`464px` desktop, `304px` mobile): centered heading, copy, orange link/button on muted background.
4. Feature section (`1097px` desktop, `1600px` mobile): heading, four stats, six feature cards in 3x2 desktop / 1-column mobile.
5. Product showcase (`1144px` desktop, `884px` mobile): heading, three pills, large application mockup. Click-driven tabs switch labels; visual clone can preserve the provider-management default state.
6. Developer section (`689px` desktop, `785px` mobile): two-column code-window and three benefit rows; stacked on mobile.
7. Testimonials (`868px` desktop, `864px` mobile): heading and two continuously moving horizontal rows of quote cards.
8. FAQ (`894px` desktop, `718px` mobile): centered heading and six single-open accordion rows.
9. CTA (`571px` desktop, `404px` mobile): full-width orange gradient, centered headline/copy/buttons/platform note.
10. Footer: brand column, four link groups, copyright and maker line.
